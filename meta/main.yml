galaxy_info:
  author: Joe Nyland
  role_name: sanoid
  namespace: joenyland
  description: Installs and configures Sanoid.
  license: MIT
  min_ansible_version: '2.1'
  platforms:
  - name: Debian
    versions:
      - trixie # 13 (stable)
      - bookworm # 12 (oldstable)
  galaxy_tags:
    - sanoid
    - zfs
dependencies:
  - role: joenyland.zfs
